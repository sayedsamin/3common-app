/**
 * Shared API boundary placeholder. Add the configured base URL from env, timeouts, typed error normalization, and response parsing when the backend is selected. Feature api.ts files should call this boundary. Do not implement fake successful requests.
 */
export {};
