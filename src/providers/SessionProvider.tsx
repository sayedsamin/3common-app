/**
 * Placeholder: implement session orchestration when an authentication backend is chosen.
 * Restore validated tokens through storage/secure-store and expose typed session state.
 * Clear user-specific Query caches and persistence on sign-out.
 * Mount the real provider in AppProviders; no fake session is supplied.
 */
export {};
