# Shared UI foundations

These minimal primitives and semantic tokens are placeholders for the eventual visual design. Import from @/components/ui. Screen is a scrollable content layout; for FlashList screens, build a non-scrolling safe-area wrapper to avoid nesting virtualized lists. Add keyboard avoidance to form screens when their behavior is known. Use Expo UI behind shared primitives when its native behavior and platform support fit.
