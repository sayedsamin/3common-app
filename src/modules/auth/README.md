# Authentication placeholder

Choose the authentication backend before implementing screens or session state. Implement the module contract from AGENTS.md and connect providers/SessionProvider.tsx and storage/secure-store.ts. Add (auth) routes and protected (app) routes when real pages exist. Never treat a placeholder session as authenticated.
