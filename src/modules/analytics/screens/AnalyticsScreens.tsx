import { PlaceholderContent, Screen } from '@/components/ui';

export function DashboardScreen() {
  return <Screen edges={['left', 'right', 'bottom']}><PlaceholderContent title="Dashboard" icon="chart-box-outline" message="In progress" /></Screen>;
}

