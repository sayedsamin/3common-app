import { PlaceholderContent, Screen } from '@/components/ui';
export function ProfileScreen() {
  return <Screen edges={['left', 'right']}><PlaceholderContent title="Profile" icon="account-outline" /></Screen>;
}
